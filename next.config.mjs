/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "cdn.dummyjson.com",
            "images.unsplash.com",
            "cdn.pixabay.com",
            "fakestoreapi.com"
        ]
    }
};

export default nextConfig;
